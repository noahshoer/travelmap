{
  "name": "travelmap-frontend-typecheck",
  "version": "0.0.0",
  "private": true,
  "description": "Dev-only: TypeScript is used solely to type-check the JSDoc-annotated web/ ES modules. Nothing is bundled or shipped.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test:e2e": "playwright test --config=e2e/playwright.config.ts"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1",
    "typescript": "5.9.3"
  }
}
