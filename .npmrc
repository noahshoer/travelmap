# Block install/postinstall lifecycle scripts (the main npm supply-chain vector).
ignore-scripts=true
# Never write ^ or ~ ranges; pin exact versions.
save-exact=true
# Don't phone home.
fund=false
audit=false
